{
  "name": "zentao-skill",
  "version": "0.1.0",
  "description": "内网禅道 HTTP 服务，支持查询、评论、指派、解决 Bug 和飞书通知",
  "type": "module",
  "scripts": {
    "serve": "npx tsx src/index.ts",
    "debug": "npx tsx --inspect-brk src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
